version: "3.9"
services:
  db:
    image: postgres
    container_name: db
    environment:
      - POSTGRES_PASSWORD=1234
      - POSTGRES_USER=postgres
      - POSTGRES_DB=dreamjob
      - PGDATA=/var/lib/postgresql/data
    volumes:
      - ./db/shortcut/data:/var/lib/postgresql/data
    restart: always
  job4j_dreamjob:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8080"
    environment:
      - JDBC_URL=jdbc:postgresql://127.20.0.2/16:5432/dreamjob
      - JDBC_USERNAME=postgres
      - JDBC_PASSWORD=1234
      - JDBC_DRIVER=driver-class-name
    container_name: job4j_dreamjob
    depends_on:
      - db